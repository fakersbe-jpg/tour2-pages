# tour2-pages
Design system extension pages for Rwamps Tours — built with impeccable design methodology. Contains /about, /tours/:slug, /destinations/:slug, /packages/:slug, /gallery, /contact pages that extend the existing tour2 design system.
